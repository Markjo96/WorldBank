<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marco.world_bank">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".activity.TopicActivity"
            android:parentActivityName=".activity.TopicActivity">

    </activity>
        <activity android:name=".activity.CountryActivity"
            android:parentActivityName=".activity.CountryActivity">

        </activity>
    <activity android:name=".activity.IndicatorActivity"
              android:parentActivityName=".activity.TopicActivity">

    </activity>
        <activity android:name=".activity.GraphActivity"
            android:parentActivityName=".activity.GraphActivity">

        </activity>
        <activity android:name=".activity.CacheActivity"
            android:parentActivityName=".activity.CacheActivity">

        </activity>
        <activity android:name=".activity.DescriptionActivity"
            android:parentActivityName=".activity.DescriptionActivity"
            android:theme="@style/AppTheme.CustomTheme">

        </activity>
    </application>

</manifest>